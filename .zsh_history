ghci
ghci
ls
rm TesteComp.jsx* testey
ls
cd ../option_questions
ls
rm *
ls
rm *
ls
cp ../text_question/* .
ls
for x in *.dart; do echo $x; done
for x in *.dart; do echo $x | sed 's\text\option'; done
for x in *.dart; do echo $x | sed 's/text/option'; done
for x in *.dart; do echo $x | sed 's\text\option\'; done
for x in *.dart; do echo $x | sed 's/text/option/'; done
for x in *.dart; do echo $(echo $x | sed 's/text/option/'); done
for x in *.dart; do mv $x $(echo $x | sed 's/text/option/'); done
l
ls
ls
cd ..
ls
mkdir Loft
cd Documents
ls
mv drones-m* mobile ws ../Loft
ls
mc painel-adm-2.0 ../Loft
mv painel-adm-2.0 ../Loft
cd ../Loft
ls
ls
cd ../../..
ls
cd Loft/bulk-lead-importer
yarn add react-dropzone
yarn rm react-dropzone
yarn -h
yarn -h |less
yarn remove react-dropzone
cd ../..
ls
ls -a
rm -rf .git
git remote add origin git@bitbucket.org:loftbr/bulk-lead-importer.git
git iniy
git init
git remote add origin git@bitbucket.org:loftbr/bulk-lead-importer.git
git add .
git checkout -b "feature/DRON-854"
git add .
git commit -m 'feat: first commit. Copan and Material UI are ready'
git config --global user.email "laura.viglioni@loft.com.br"
 git config --global user.name "Laur√a"
gitt commit -m 'feat: first commit. Copan and Material UI are ready'
git commit -m 'feat: first commit. Copan and Material UI are ready'
git push
git push --set-upstream origin feature/DRON-854
git checkout master
git branch
git checkout -b master
git add README.md
git push
 git push --set-upstream origin maste
git commit -m 'feat: first master commit'
git add README.md
git commit -m 'feat: first master commit'
 git push --set-upstream origin maste
git branch
git pull origin master
git checkout -b "feature/DRON-849"
yarn add react-dropzone
yarn build
cd ../..
yarn build
yarn add papaparse
npm install --save material-ui-dropzone
npm install papaparse
npm install axios
node -v
nvm use 10
cd ../..
git branch
git add .
git add .
git commit -m 'feat: created a csv importer component and treated its data into a json'
git add .
git commit -m 'feat: created a csv importer component and treated its data into a json'
git add .
git commit -m 'feat: created a csv importer component and treated its data into a json'
git add .
git commit -m 'feat: created a csv importer component and treated its data into a json'
git add .
git commit -m 'feat: created a csv importer component and treated its data into a json'
git add .
git commit -m 'feat: created a csv importer component and treated its data into a json'
git add .
git commit -m 'feat: created a csv importer component and treated its data into a json'
git add .
git commit -m 'feat: created a csv importer component and treated its data into a json'
git add .
git commit -m 'feat: created a csv importer component and treated its data into a json'
git add .
git commit -m 'feat: created a csv importer component and treated its data into a json'
git add .
git commit -m 'feat: created a csv importer component and treated its data into a json'
git commit -m 'feat: created a csv importer component and treated its data into a json' --no-verify
git push
git push --set-upstream origin feature/DRON-849
git push --set-upstream origin feature/DRON-849
git pull origin master
git pull
git branch --set-upstream-to=origin/<branch> feature/DRON-849
git branch --set-upstream-to=origin/feature/DRON-849
git push
git push --set-upstream origin feature/DRON-849
